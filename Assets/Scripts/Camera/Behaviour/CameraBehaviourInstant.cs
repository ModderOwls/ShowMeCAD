using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraBehaviourInstant : CameraBehaviour
{
    //As default camera behaviour is already instant no need to add code.
    //Just here as a wrapper to avoid confusion.
}
